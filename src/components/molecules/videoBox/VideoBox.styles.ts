import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(({ palette }) => ({
  shareContainer: {
    display: 'flex',
    alignContent: 'center',
    paddingTop: '8px',
  },
}));
